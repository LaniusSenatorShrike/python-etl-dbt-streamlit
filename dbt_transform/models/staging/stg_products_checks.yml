version: 2

models:
  - name: stg_products
    description: "data quality checks for the model"
    columns:
      - name: surrogate_key
        description: "Unique identifier for each record"
        data_tests:
          - unique
          - not_null